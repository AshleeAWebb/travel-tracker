class Traveler {
  constructor(travelerData, tripManager, destinationManager) {
    this.id = travelerData.id;
    this.name = travelerData.name;
    this.travelerType = travelerData.travelerType;
  }
}
export default Traveler